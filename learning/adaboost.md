Start from a simple classifer, and run classification problem using different algorithm many times, 
with weights adjusted for each iteration. The outlier data get more weights, and conforming data don't.
